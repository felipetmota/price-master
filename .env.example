# URL of the on-premise REST API (Node.js + SQL Server) used in production.
# Leave blank in the Lovable preview — the app will fall back to the
# bundled .xlsx template loader so it stays fully interactive.
#
# Example: VITE_API_URL=http://price-api.intranet:3001
VITE_API_URL=